array := [0,1,2,3,4,5]
double_slice := array[1..4][1..2]

print("After slicing twice, the result should be 2,3:", double_slice)

called := 0
Slice:::_at = Slice.at
Slice:::at = |index| {
	called = called + 1
	this._at(index)
}

print("If the double slice works correctly, this will be 3:", double_slice[1])
print("Slice.at is called", called, "times on each access (should be once).")
